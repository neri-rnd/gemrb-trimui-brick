diff --git a/gemrb/core/DialogHandler.cpp b/gemrb/core/DialogHandler.cpp
index fe8b9eb..9229d28 100644
--- a/gemrb/core/DialogHandler.cpp
+++ b/gemrb/core/DialogHandler.cpp
@@ -159,6 +159,7 @@ void DialogHandler::EndDialog(bool try_to_break)
 		// reset the TA
 		ta->SetSpeakerPicture(nullptr);
 		ta->ClearSelectOptions();
+		ta->ClearDialogStart();
 	}
 
 	Actor* tmp = GetSpeaker();
@@ -431,6 +432,8 @@ bool DialogHandler::DialogChoose(unsigned int choose)
 		return false;
 	}
 
+	ta->MarkDialogStart();
+
 	if (target) {
 		// displaying npc text and portrait
 		if (tgta) {
diff --git a/gemrb/core/GUI/TextArea.cpp b/gemrb/core/GUI/TextArea.cpp
index 878d802..16235a1 100644
--- a/gemrb/core/GUI/TextArea.cpp
+++ b/gemrb/core/GUI/TextArea.cpp
@@ -346,7 +346,11 @@ void TextArea::UpdateScrollview()
 
 		if (core->HasFeature(GFFlags::DIALOGUE_SCROLLS)) {
 			anim = 500;
-			y = 9999999; // FIXME: properly calculate the "bottom"?
+			if (dialogScrollTarget >= 0) {
+				y = dialogScrollTarget;
+			} else {
+				y = nodeBounds.y - LineHeight();
+			}
 		} else {
 			int blankH = frame.h - LineHeight() - nodeBounds.h - optH;
 			if (blankH > 0) {
@@ -615,6 +619,11 @@ void TextArea::AddSubviewInFrontOfView(View* front, const View* back)
 	View::AddSubviewInFrontOfView(front, target);
 }
 
+void TextArea::MarkDialogStart()
+{
+	dialogScrollTarget = textContainer ? textContainer->Dimensions().h : 0;
+}
+
 int TextArea::TextHeight() const
 {
 	return textContainer ? textContainer->Dimensions().h : 0;
@@ -741,6 +750,7 @@ void TextArea::TextChanged(const TextContainer& /*tc*/)
 
 void TextArea::ClearText()
 {
+	dialogScrollTarget = -1;
 	delete scrollview.RemoveSubview(textContainer);
 
 	parser.Reset(); // reset in case any tags were left open from before
diff --git a/gemrb/core/GUI/TextArea.h b/gemrb/core/GUI/TextArea.h
index c166e2b..6ec3dd5 100644
--- a/gemrb/core/GUI/TextArea.h
+++ b/gemrb/core/GUI/TextArea.h
@@ -180,6 +180,9 @@ public:
 	void SetMargins(ContentContainer::Margin m);
 	ScrollBar* GetScrollBar() const;
 
+	void MarkDialogStart();
+	void ClearDialogStart() { dialogScrollTarget = -1; }
+
 	/** Returns the selected text */
 	String QueryText() const override;
 	/** Marks textarea for redraw with a new value */
@@ -193,6 +196,7 @@ private: // Private attributes
 	// dialog and listbox handling
 	std::vector<value_t> values;
 	const Content* dialogBeginNode;
+	int dialogScrollTarget = -1;
 	Holder<Sprite2D> speakerPic;
 	// dialog options container
 	SpanSelector* selectOptions = nullptr;
